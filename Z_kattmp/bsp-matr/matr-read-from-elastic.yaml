

die callbacks stehen NICHT in der OAS!
bei den Daten, die zurückgegeben werden in der Response steht in der jeweilige operation einfach
desription: >
    'das ist das feld xyz ... 
    from elasticSearch'


properties:
                    mount-name:
                      type: string
                      description: >
                        'Identifier of the device in the controller, identical with NetworkElementID at Telefonica Germany
                        from elasticsearch'
                    own-mac-address:
                      type: string
                      description: >
                        'MAC address of the device that holds the MAC address table
                        from elasticsearch'
                    egress-ltp-uuid:
                      type: string
                      description: >
                        'UUID of the MacInterface towards the listed remote MAC addresses
                        from elasticsearch'
                    original-ltp-name:
                      type: string
                      description: >
                        'Name of the egress interface as it is represented on the Local Craft Terminal provided by the vendor
                        from elasticsearch'
                    vlan-id:
                      type: integer
                      description: >
                        'VLAN that connects the listed remote MAC addresses
                        from elasticsearch'
                    remote-mac-address:
                      type: string
                      description: >
                        'Remote MAC address that can be reached at the egress interface and inside the VLAN
                        from elasticsearch'
                    time-stamp-of-data:
                      type: string
                      description: >
                        'Date and time when the data has been retrieved from the device
                        from elasticsearch'